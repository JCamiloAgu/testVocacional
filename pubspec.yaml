name: testvocacional
description: Aplicacion para el Sena, la cual busca darle a las personas una idea sobre cual carrera empezar a estudiar.
version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^4.3.2+2
  cupertino_icons: ^1.0.0
  pedantic: ^1.8.0
  charts_flutter: ^0.9.0
  http: ^0.12.2
  percent_indicator: ^2.1.8
  edge_alert: ^0.0.1
  mailer: ^3.2.1
  progress_dialog: ^1.2.4
  permission_handler: ^5.0.1+1
  open_file: ^3.0.1
  ext_storage: ^1.0.3

dev_dependencies:
  flutter_test:
    sdk: flutter
flutter:
  uses-material-design: true
